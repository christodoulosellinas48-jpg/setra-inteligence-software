{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user"
      ],
      "default": "user"
    },
    "has_seen_tour": {
      "type": "boolean",
      "default": false,
      "description": "Whether the user has completed or skipped the guided onboarding tour"
    }
  },
  "required": []
}